## Connected workflow verification

The desktop entry screen retains the ResQplan command-surface identity, role cards, demo credential path, simulated-data disclosure, and the new workflow-ready session entry. The mobile capture remains readable at 375px: the editorial hero stacks above the login panel, role cards remain tappable, and the sign-in controls remain visible without horizontal overflow. TypeScript checks and the production build pass. The dashboard workflow is implemented in shared state but requires authenticated interaction to exercise manually in the preview.

## Fund & Recovery extension verification

The existing ResQplan entry surface remains visually unchanged after the feature extension. Desktop review at 1280×720 preserved the split command-surface composition, navy/mineral-white contrast, lifecycle rail, login role selection, and simulated GIS briefing image. Mobile review at 390×844 preserved readable hierarchy, stacked role cards, usable form controls, and no horizontal overflow in the entry experience.

The Fund & Recovery feature is implemented as an Authority-only nested recovery surface. Its values are derived from the persisted simulated project dataset; remaining funding is always calculated from approved minus utilized. Evidence is kept behind View Evidence, uploaded files are represented as demo-only evidence register entries, and delayed work uses a clear warning without adding a notification system.
